{
  "name": "labnotebook-app",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "lint": "npm --prefix web run lint",
    "test:e2e": "npm --prefix web run test:e2e",
    "screenshots": "npm --prefix web run screenshots"
  }
}
